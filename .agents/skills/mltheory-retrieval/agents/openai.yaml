interface:
  display_name: "MLTheory Retrieval"
  short_description: "Goal-driven retrieval with strict local->loogle->external order"
  default_prompt: "Use $mltheory-retrieval to build a verified candidate set for this MLTheory goal with strict local-first retrieval."

dependencies:
  tools:
    - type: "mcp"
      value: "lean-lsp-mcp"
      description: "Lean LSP MCP server"

policy:
  allow_implicit_invocation: false
