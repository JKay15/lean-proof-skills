interface:
  display_name: "MLTheory Retrieval"
  short_description: "Domain-profile retrieval with progressive widening and strict existence checks"
  default_prompt: "Use $mltheory-retrieval to build a verified candidate set for this MLTheory goal using domain-local -> slice -> widening -> global order."

dependencies:
  tools:
    - type: "mcp"
      value: "lean-lsp-mcp"
      description: "Lean LSP MCP server"

policy:
  allow_implicit_invocation: false
