interface:
  display_name: "ML Paper Workflow"
  short_description: "Strict 6-step Lean construction wrapper over $lean4"
  default_prompt: "Use $ml-paper-workflow to execute Snapshot -> Retrieval -> Screening -> Minimal Patch -> Gate -> Artifact update for this Lean task card, and delegate proof cycles to $lean4."

dependencies:
  tools:
    - type: "mcp"
      value: "lean-lsp-mcp"
      description: "Lean LSP MCP server"

policy:
  allow_implicit_invocation: false
