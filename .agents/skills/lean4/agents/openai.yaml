interface:
  display_name: "Lean 4"
  short_description: "Lean4 proving workflow with LSP and mathlib search"
  default_prompt: "Use $lean4 to prove or repair this Lean goal with mathlib-first search and build validation."

dependencies:
  tools:
    - type: "mcp"
      value: "lean-lsp-mcp"
      description: "Lean LSP MCP server"

policy:
  allow_implicit_invocation: true
