interface:
  display_name: "MLTheory Upgrade"
  short_description: "Dedicated mathlib upgrade workflow with graph/index regression checks"
  default_prompt: "Use $mltheory-upgrade to run a dedicated mathlib upgrade workflow for MLTheory, including pre/post artifact validation and rollback checkpoints."

dependencies:
  tools:
    - type: "mcp"
      value: "lean-lsp-mcp"
      description: "Lean LSP MCP server"

policy:
  allow_implicit_invocation: false
